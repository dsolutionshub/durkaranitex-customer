export const metadata = {
  title: "Replacement Policy",
  description:
    "Kavya Creation replacements only — request within 1 day of delivery for damaged sarees. No refunds.",
};

export default function ReplacementPolicyLayout({ children }) {
  return children;
}
